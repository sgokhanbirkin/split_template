part of home_view;

class HomeViewBottom extends StatelessWidget {
  const HomeViewBottom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
